# NOTICE — tools/

Everything under `tools/` is **SUSE-specific content** and is **NOT** covered by
the repository-root MPL-2.0 license (`/LICENSE`).

These tool definitions (manifests, templates, hooks, styles, thumbnails, and
local assets) are **proprietary to SUSE — all rights reserved**. They are slated
to move to a private repository as part of the open-sourcing plan, under which
only `engine/`, `shells/`, `schemas/`, and `docs/` are open-sourceable while
`tools/` and `catalog/assets/` stay private. See the "Open-sourcing plan"
section of the root `README.md` and `SOVEREIGNTY.md`.

## Exception: tool icons

Each tool's `icon.svg` follows the Lucide icon house style. Lucide is licensed
under the **ISC** license. See `/THIRD-PARTY-NOTICES.md` for the full attribution
and license text.
