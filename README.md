# lolly-suse-tools

Extracted from the [`lolly`](https://github.com/lolly-tools/lolly) monorepo and
consumed there as a git submodule at `tools/`.

Tool/catalog **data**, not code. See `NOTICE.md` for licensing.
